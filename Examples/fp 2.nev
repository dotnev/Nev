main :: () {
  nums := [1, 2, 3, 4, 5, 6]
  nums.for_each(x) => cmd.print("${x}: hi")
  // also map filter is fp features
}

// How it looks like:
Array<T> <<
  @for_each :: (f: (T)) {
    for item in me.vals => f(item)
  }
